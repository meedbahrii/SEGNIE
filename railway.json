{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "export NPM_CONFIG_CACHE=/tmp/.npm-cache; export NPM_CONFIG_TMP=/tmp; rm -rf /app/node_modules/.cache || true; npm ci --omit=dev --no-audit --no-fund --no-progress && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start"
  }
}
